version: '3'
services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
  node:
    build:
      dockerfile: Dockerfile.dev
      context: ./
    restart: always
    volumes:
      - ./app/node_modules
      - ./src:/app/src
      - ./test:/app/test
